import { useToasts } from "@/contexts/toasts/ToastContextValue";

export default function HomePage() {

    return (
        <div className="min-h-screen flex items-center justify-center bg-background">
            HELLO WELCOME HOME
        </div>
    );
}